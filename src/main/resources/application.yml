spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dreamdays?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: lisa7393

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update  # 운영 환경에서는 'update' 또는 'none'이 좋습니다.
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect



